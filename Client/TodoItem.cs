public class TodoItem       //creates a class for each todo object to use
{
    public string Title { get; set; }   //creates a string property to get/set the task name
    public bool IsDone { get; set; }    //creates a boolean property to get/set if the task is done or not
}